{{#each this}}
 <div class="main-section-card">
    <img 
    src="{{backdrop_path}}" 
    class="card-top-img"
    alt="{{title}}" 
    />
     <div class="card-body">
         <h5 class="card-title">{{title}}</h5>
         <p class="card-text">{{genre_ids}}|{{release_date}} <span class="vote-card">{{vote_average}}</span></p>
     </div>
 </div>
{{/each}}